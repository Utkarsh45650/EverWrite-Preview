# EverWrite Frontend

This folder now contains the React + Vite frontend for EverWrite.

## Development

```bash
cd frontend
npm install
npm run dev
```

## Production build

```bash
cd frontend
npm run build
```

The app expects the Flask backend API to be available at the configured `API_BASE_URL`.
